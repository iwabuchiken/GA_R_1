<%= form_for(@admin) do |f| %>
  <% if @admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
      <% @admin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :current_generation %><br />
    <%= f.number_field :current_generation %>
  </div>
  <div class="field">
    <%= f.label :num_of_gene_elements %><br />
    <%= f.number_field :num_of_gene_elements %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
