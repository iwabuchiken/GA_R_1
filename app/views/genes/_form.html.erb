<%= form_for(@gene) do |f| %>
  <% if @gene.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gene.errors.count, "error") %> prohibited this gene from being saved:</h2>

      <ul>
      <% @gene.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :suited %><br />
    <%= f.number_field :suited %>
  </div>
  <div class="field">
    <%= f.label :gene %><br />
    <%= f.text_area :gene %>
  </div>
  <div class="field">
    <%= f.label :generation %><br />
    <%= f.number_field :generation %>
  </div>
  <div class="field">
    <%= f.label :in_generation_serial %><br />
    <%= f.number_field :in_generation_serial %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
